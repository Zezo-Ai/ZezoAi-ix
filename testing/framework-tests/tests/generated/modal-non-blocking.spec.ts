/*
 * SPDX-FileCopyrightText: 2025 Siemens AG
 *
 * SPDX-License-Identifier: MIT
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */

/**
 *  This test was autogenerated by 'testing/framework-tests/src/main.ts'
 *  Extended manually: open the dialog before the aria snapshot (generator only produces load + snapshot).
 */
import { test, expect } from '@playwright/test';
import { waitForReadiness } from '../utils';

test('modal-non-blocking', async ({ page }) => {
  await page.goto('/preview/modal-non-blocking');

  // Ugly and not the reliable way to wait for Stencil to be ready
  await waitForReadiness(page);

  await page
    .getByRole('button', { name: 'Show non-blocking modal' })
    .click();
  await expect(page.getByRole('dialog')).toBeVisible();

  await expect(page.locator('body')).toMatchAriaSnapshot({
    name: 'modal-non-blocking.aria-snapshot.yaml',
  });
});
